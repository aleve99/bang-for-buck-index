/**
 * Database row contracts.
 * Generated Supabase types are not used in this environment (native Postgres + Drizzle).
 * These re-exports are the canonical schema types for app code.
 */
export type {
  Beer,
  Country,
  ExchangeRate,
  NewPriceEntry,
  PriceEntry,
} from "@/db/schema";
